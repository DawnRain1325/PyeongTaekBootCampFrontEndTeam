보통2월은 2월 28일 까지 있으나

몇년에 한번 29일 까지 있는 경우도 있음.

leap Year (윤년) 이라고함

-4로 나누어 떨어지는 해는 윤년

- 100으로 나누어 떨어지면 윤년이 아님

-400 으로 나누어 떨어지면 윤년

function 함수 (매개변수) {
let output = 초깃값
처리한다
return output
}

호출할때 매개변수의 개수가 고정적이지않은 함수를 가변 매개변수 함수라고 부릅니다.

자바스크립트에서 이러한 함수를 구현할때는 나머지 매개변수 rest parameter 라는 특이한 형태의 문법을 사용합니다.

function 함수 이름 (... 나머지 매개변수) {}

나머지 매개변수는 이름 그대로 나머지

function 함수 이름 (매개변수 , 매개변수 , ... 나머지 매개변수){}

전개 연산자

함수이름 (...배열) 

전개 연산자를 사용하면 숫자 하나하나 들어옴.

기본 매개변수 

항상 같은 매개변수를 여러번 반복해서 입력하는것이 귀찮을 경우에 매개변수에 기본값을 지정하는 기본 매개변수를 사용

함수이름 (매개변수 , 매개변수=기본값 , 매개변수=기본값)

매개변수는 왼쪽부터 입력하므로 다음과 같이 함수를 작성하면 기본매개변수의 읨가 없습니다.
b에 값을 전달하기 위해서는 a에값을 채워야 하기 떄문입니다.

구버전 자바스크립트에서 가변 매개변수 함수를 구현할때는 배열내부에서 사용할수 있는 특수한 변수인

arguments 를 활용합니다.

